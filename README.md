# Виртуальная файловая система (Composite Pattern)

## Описание задачи

В этом задании вы реализуете иерархию объектов для моделирования файловой системы. Вы познакомитесь с паттерном **Composite (Компоновщик)** — одним из структурных паттернов проектирования.

### Суть паттерна

Паттерн Composite позволяет объединять объекты в древовидные структуры и работать с ними как с едиными объектами. В нашей задаче:

- **Файл** — это "лист" дерева (не может содержать других элементов)
- **Папка** — это "узел" дерева (может содержать файлы и другие папки)

Главная "изюминка" задачи: метод [`getSize()`](include/Node.hpp:11) ведёт себя по-разному для файлов и папок:
- Для файла — просто возвращает его размер
- Для папки — рекурсивно обходит всё внутри и считает общий вес

---

## Структура проекта

```
.
├── include/
│   ├── Node.hpp          # Базовый абстрактный класс
│   ├── File.hpp          # Заголовок класса File
│   └── Directory.hpp     # Заголовок класса Directory
├── src/
│   ├── File.cpp          # Реализация File (нужно заполнить)
│   └── Directory.cpp     # Реализация Directory (нужно заполнить)
├── solution/
│   ├── File.cpp          # Готовое решение для File
│   └── Directory.cpp     # Готовое решение для Directory
├── tests/
│   └── test_runner.cpp   # Тесты для проверки
├── CMakeLists.txt        # Конфигурация сборки
└── README.md             # Этот файл
```

---

## Что нужно реализовать

### 1. Класс `File`

Файл [`File.cpp`](src/File.cpp) должен содержать реализацию:

```cpp
File::File(std::string name, size_t size);
size_t File::getSize() const;
```

**Подсказка:** Это самый простой класс — просто сохраните размер в конструкторе и возвращайте его в методе.

---

### 2. Класс `Directory`

Файл [`Directory.cpp`](src/Directory.cpp) должен содержать реализацию:

```cpp
Directory::Directory(std::string name);
void Directory::add(std::shared_ptr<Node> node);
size_t Directory::getSize() const;
```

**Подсказка для `add()`:**
- Просто добавьте узел в вектор: `children.push_back(node);`
- **Дополнительно (уровень 6/10):** Проверьте на nullptr перед добавлением для защитного программирования

**Подсказка для `getSize()`:**
1. Создайте переменную для суммарного размера
2. Пройдитесь по всем элементам в `children`
3. Для каждого вызовите `getSize()` — **полиморфизм сам разберётся**, файл это или папка
4. Верните сумму

---

## Пример использования

```cpp
// Создаём структуру:
// Root (Folder)
// ├── doc.txt (100 bytes)
// └── Images (Folder)
//     ├── photo.png (500 bytes)
//     └── icon.png (50 bytes)

auto root = std::make_shared<Directory>("root");
auto file1 = std::make_shared<File>("doc.txt", 100);

auto imgDir = std::make_shared<Directory>("images");
auto img1 = std::make_shared<File>("photo.png", 500);
auto img2 = std::make_shared<File>("icon.png", 50);

imgDir->add(img1);
imgDir->add(img2);

root->add(file1);
root->add(imgDir);

// Размер root = 100 + 500 + 50 = 650
std::cout << root->getSize() << std::endl;  // 650
```

---

## Сборка и запуск тестов

### Локально (студенческая реализация)

```bash
# Настройка проекта
cmake -S . -B build

# Сборка
cmake --build build

# Запуск тестов
./build/test_runner
```

**Примечание:** Стартовый код содержит TODO-комментарии. Тесты будут проходить только после заполнения реализаций в [`src/File.cpp`](src/File.cpp) и [`src/Directory.cpp`](src/Directory.cpp).

### Проверка с готовым решением

Для проверки правильности реализации можно использовать готовое решение:

```bash
# Копируем решение в src
cp solution/File.cpp src/File.cpp
cp solution/Directory.cpp src/Directory.cpp

# Пересобираем
cmake --build build

# Запускаем тесты
./build/test_runner
```

### С GitHub Actions

Тесты запускаются автоматически при каждом push в репозиторий через файл [`.github/workflows/classroom.yml`](.github/workflows/classroom.yml).

---

## Что вы узнаете

1. **Полиморфизм** — один интерфейс, разная реализация
2. **Рекурсия** — обход древовидных структур
3. **Умные указатели** — `std::shared_ptr` для управления памятью
4. **Паттерн Composite** — как строить древовидные структуры

---

## Тесты

В файле [`test_runner.cpp`](tests/test_runner.cpp) реализованы следующие тесты:

| Тест | Описание |
|------|----------|
| `testFileSystem()` | Основной тест с вложенной структурой |
| `testEmptyDirectory()` | Проверка пустой папки |
| `testDeepNesting()` | Глубокая вложенность папок |
| `testMultipleFiles()` | Несколько файлов в папке |
| `testMixedContent()` | Смешанное содержимое (файлы + папки + пустые папки) |
| `testZeroSizeFile()` | Проверка файлов нулевого размера (важно для корректной логики) |
| `testNullptrHandling()` | Проверка обработки nullptr (защитное программирование) |

### Функция проверки

Для информативных сообщений об ошибках используется вспомогательная функция:

```cpp
void check(bool condition, const std::string& message);
```

При провале теста выводит сообщение и завершает программу с кодом ошибки 1 (для автопроверки в CI/CD).

---

## Уровень сложности: 5/10 (до 6/10 с дополнительными тестами)

- **Простой (5/10):** Понимание наследования и виртуальных функций
- **Средний (5/10):** Работа с умными указателями
- **Интересный (5/10):** Рекурсивный полиморфизм в действии
- **Продвинутый (6/10):** Защитное программирование (обработка nullptr, файлов нулевого размера)

Удачи! 🚀
